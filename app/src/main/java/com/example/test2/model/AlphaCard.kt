package com.example.test2.model

class AlphaCard {
    var icons:Int ? = 0
    var text:String ? = null

    constructor(icons: Int?, text: String?) {
        this.icons = icons
        this.text = text
    }
}